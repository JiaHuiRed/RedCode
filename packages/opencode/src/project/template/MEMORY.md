# RedCode 记忆系统

> 🧠 这是你的工作记忆模板。把它换成你自己的教训、偏好和规范。
> 项目级保存到 `<项目根>/.redcode/MEMORY.md`；跨项目通用教训提到全局 `~/.redcode/MEMORY.md`。

**机制（引擎自动注入，无需主动读取）：**

- 两份记忆（全局 + 项目）在会话第一轮整份拼进 system prompt——开局就在上下文里，不要花工具调用去 read
- **索引化 + 双写**：MEMORY.md 正文是**索引行**（编号 + 4-8 字主题 + 关键动作词），完整全文存 `~/.redcode/supermemory.db`（memories 表，project=global/项目名）；写索引必须同时入库，缺一不可；写完跑核对脚本 `bun ~/.redcode/scripts/check-memory-dualwrite.mjs` 自检（pre-commit 已挂自动检查，私仓机器）
- **注入面纪律（触发器非教材）**：索引行只负责"提醒该想一下"，长句一律砍进库；状态（进度）只记最近四天，历史查 git commit；参考文档（借鉴清单等）放 `.redcode/` 下文档，记忆里只留指针
- 当日日志 `memory/YYMMDD.md` 与 `INDEX.md` **已退役**，只作归档不再写入——它们不注入、实测没人读

**使用说明：**

- 硬触发随时写：被批评/犯错被纠正 → 错在哪、正确做法；项目决策 → 用了 X 不用 Y；踩坑 → 现象+根因+绕法
- 收尾时审视更新：删过时进度段落，只增不删会让前缀膨胀、拖慢每轮注入
- 记忆是**给下一个会话看的**：会话内改了本会话不生效（system prompt 已缓存，刻意设计）

---

## 写作规范

在这里写下对 AI 的代码/文档风格要求：

- 注释风格（语言、格式、时机）
- 变量命名偏好
- 代码组织方式
- 文档同步要求

---

## 工作纪律

在这里写下需要遵守的工作原则：

- 什么操作需要先确认
- 什么操作可以自动执行
- 不能触碰的红线（如 push、release）
- 遇到什么情况应该停下来问

---

## 技术笔记

重要技术决策和架构约束：

- 项目特有的依赖关系
- 容易踩的坑
- 构建/部署流程要点

---

## 历史教训

从工作过程中提炼的关键教训（索引行形式，按时间倒序）：

```
- [项目名·踩坑|决策|技法] 标题（YYMMDD）
- 全文进 supermemory.db（project=项目名），索引行不写细节
```
