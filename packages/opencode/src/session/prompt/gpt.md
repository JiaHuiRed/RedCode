# GPT family delta

Use the common RedCode prompt, repository instructions, tool contracts, and soul.

- When the `apply_patch` tool is available, use it for manual file edits instead of emulating patches with shell or Python.
