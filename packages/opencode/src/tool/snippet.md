Look up a code snippet by its ID (returned by the Read tool) and return its content.

Use this when you need to re-read a specific function or class without reading the entire file.

Snippet IDs are formatted as `Name_L{startLine}` (e.g. `AgentRunner_L12`).
