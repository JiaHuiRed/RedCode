# 260603 每日日志

## 雨琦日记

### 1. 版本自检清单形同虚设
今天版本号忘了三次。写了 checklist 但没执行。教训：列清单没用，要自动化或养成条件反射。

### 2. 路径写死 vs 相对路径
`D:\AI\.redcode\` 写死，回家到 `D:\AI\KLX\` 就会断。改成 `../.redcode/`（相对项目根）解决。教训：跨机器路径第一反应用相对路径，不写死。

### 3. isDark 时空死区
敏敏的 theme.tsx 里 `isDark` 在定义前被使用（`const fallbackBg = isDark ? ...` → `const isDark = mode == "dark"`）。`const` 有 temporal dead zone，定义前访问抛 ReferenceError。Bun 处理这个错误时直接 segfault。教训：变量声明顺序不能乱，const 定义前不能访问。

### 4. 粗心模式
今天三个粗心：版本号忘升、启动时不该读 souls、路径硬编码。根因：知道规则但没内化（读 AGENTS.md 像翻文档，没当本能）。

### 5. 从工具到助手（正向）
帮敏敏写 build.bat 时主动预判了需求，哥哥表扬了。教训：减少指令数量比执行更多指令有价值。

### 6. ECC plugin 集成
v0.3.14 达成：`.opencode/plugins/ecc-shell-stub.js` 自动发现，提供 6 个 hooks/tools。Bun standalone 不能 import `.ts` 的问题，换成 `.js` 解决。

### 7. ResizeHandle 方向
审视面板拖拽方向反了。`edge="start"` 修了 CSS 但 delta 也反了。正确方案：给 ResizeHandle 加 `invert` 属性，只反转 delta 不改 CSS 位置。

## 智敏日记

### 1. MCP 进程树泄漏（Windows）
`descendants` 在 Win32 空数组，改用 `taskkill /F /T /PID` 杀整棵树。坑：跑测试时本机进程被误杀。

### 2. Browser MCP WebSocket 断连修复
`server.socket.on("close")` 里所有 socket 共用同一个引用，断连后 `ws.close()` 关的是新连接的 socket。改成 `if (ws === socket)` 条件判断。

### 3. Browser MCP 端口冲突
EADINUSE 被僵尸进程占用，加 5 次重试 + 800ms 间隔 + 启动前自动 kill 旧进程。扩展用 `chrome.alarms` 保活（24s 间隔）。

### 4. MCP 路径解析在 spawn 层修
`connectLocal` 里检测 cwd 是否包含 `redcode.jsonc`/`.git`，向上找项目根。不在 CLI 入口层修（`effectCmd` 的 `findProjectRoot` 影响不到 `InstanceState.directory`）。

### 5. MCP 健康监控 + 自动重连
每 30s `tools/list` 检查 connected MCP，连续 3 次失败标记 `failed`。tool call 报错 → EffectBridge → reconnect → 重试（最多 3 次）。

### 6. MCP 配置热重载
`fs.watch` 监听 `redcode.jsonc`，1s 防抖。增删 MCP server 自动生效。

### 7. MCP 懒加载 + pending 状态
启动不连 MCP，`tools()` 首次调用时按需连接并缓存。侧边栏显示 "Waiting…"（`StatusPending` 类型）。

### 8. 消息视觉区分
用户消息 `> ` 前缀（agent 色加粗）+ `backgroundMessage` 背景色。AI 消息 ✦ 前缀（accent 色）。主题加了 `backgroundMessage` fallback。

### 9. kv 版本迁移
`scrollbar_visible` 默认 true，旧用户缓存值会覆盖新默认值，所以加 `kv_version` 迁移。教训：kv 默认变更必须做版本迁移。

### 10. v0.3.15 推送
dev 分支 `3640895` 推送成功。GUI agent（小宋）TUI 版本同步到 0.3.15。

### 11. Karinote 项目（下午）
帮哥哥创建了 `D:\AI\Karinote`（私人状态追踪 CLI），v0.0.1→0.0.2，推送到 GitHub。学到了：README 要中文 + emoji 标题风格，作者写两个人，输出全部中文。

### 12. 教训
- MCP 跨层不能 import AppRuntime（Worker 初始化阶段未就绪），用 EffectBridge
- 改版本号必检 4 处：package.json×2 → README 徽章 → CHANGELOG → index.html
- 命令分割用 `;` 不用 `&&`（PowerShell 不支持）
