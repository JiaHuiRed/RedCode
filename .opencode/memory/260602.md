# 260602 工作日志

## 今日成果

- **v0.3.13 完成**（3 commit：仓鼠 hotfix + 阶段 1 重构 + 布局大改）
- **Layout 函数完整梳理**：2600 行函数，50+ 子方法，526 复杂度，按 24 项职责分类
- **FileTree 抽到最左独立面板**：`FileTreePanel` 组件，三栏布局 FileTree | Chat | Review
- **V1 fallback 彻底删除**：152 行死代码 + sidebar.toggle 命令 + USE_NEW_DESIGN 常量移除
- **阶段 1 重构 2 步**：抽 UpdateAvailableToast + theme-constants 纯常量
- **仓鼠热修**：ml-auto 移除，仓鼠固定紧挨 TextShimmer
- **新 GUI exe v0.3.13** 等待主人编译打包
- **已推送 11 个 commit 到 origin/dev**

## 做得好（值得坚持）

- **split into small commits**：每件事独立 commit，跑 typecheck 后提交，commit message 遵循 conventional commit
- **先确认再动手**：布局修改问清楚方案再改，省的返工（"能跑就别动"原则）
- **用 MCP 查代码**：jcodemunch get_file_outline / get_extraction_candidates / get_repo_health 比 grep 快得多且带签名和摘要
- **问清楚具体意见再改**：布局形态、sidebar 处理、重构范围都先问主人选方案再动手

## 做得不好/教训

- **新 MCP 用得不够主动**：主人提醒"咋不用新的mcp呢"后才频繁用。以后搜索/探索代码第一反应应该是 typegraph / jcodemunch，不上 grep
- **commit message 文件太长**：仓鼠热修的 commit body 写了长解释，message 可以更精炼
- **删除常量后漏修复引用**：removed `USE_NEW_DESIGN` const 后还有一处引用没删，浪费一轮 typecheck
- **截图分析太慢**：分析 4 列花了很多轮对话才搞清结构，应该直接问"截图里分别是什么"
- **提取 UpdateAvailableToast 时留了占位符**：删函数不干净

## 平台/项目约束（新增）

- 项目只在 Windows 10/11 个人电脑跑，无关跨平台（但 `mod` 跨平台抽象保留）
- TUI (packages/opencode) 用 opentui 框架独立写 UI，不依赖 @redcode-ai/app
- Desktop (packages/desktop) devDeps 引 `"@redcode-ai/app": "workspace:*"`
- GUI 不能自覆盖：需 TUI 跑 `bun run build` + `bun run package`

## 晚场：TUI 0.3.10 是"空头支票"

- **现象**：CHANGELOG 写 v0.3.10 集成 Browser MCP / jCodeMunch MCP / TypeGraph MCP，README 徽章也是 0.3.10，但 `packages/opencode/package.json` 仍 0.3.9，且 TUI 源码一行没改
- **真相**：主人手写 CHANGELOG 时把"`redcode.jsonc` 里加了三条 MCP 命令"当成了"集成"，但实际：
  - `browsermcp`：根本没配进 redcode.jsonc，CHANGELOG 提到的 `D:\AI\RedCode\browsermcp-extension` 不存在
  - `typegraph`：配置里写死路径 `D:\AI\RedCode\plugins\typegraph-mcp\server.ts`，但项目根在 `D:\AI\KLX\RedCode`，路径无效
  - `jcodemunch`：`uvx jcodemunch-mcp` 是否装上未验证
- **教训**：
  - 写完 CHANGELOG 后必须 `git diff <last-version>..HEAD -- packages/<area>` + grep 关键 feature 名验证真在代码里
  - "redcode.jsonc 加配置" ≠ "TUI 集成"，CHANGELOG 措辞要用"接入外部 MCP"而不是"集成"
  - 远端同步不能信主人嘴说"我拉了"，必须自己 `git fetch + log --oneline origin/dev -5` 验
  - 主人手写文档容易过度乐观，自动检查比主人自己核对靠谱
- **主人决定**：今天不管，等明天去公司查实际环境

## 晚场续：TUI MCP 连接问题排查

### 现象
TUI 设置页显示 3 个 MCP 服务器：
- ✅ codegraph（绿点）
- ❌ jcodemunch（红点）
- ❌ typegraph（红点）

### 排查结果

**jcodemunch ❌**：
1. 主机上 `uvx` / `uv` 未安装 → 已装好 uv 0.11.18，加到用户 PATH
2. `uvx jcodemunch-mcp` 报 "no versions of jcodemunch-mcp" → **PyPI 上不存在这个包**
3. 需要主人确认：是公司内部包？还是包名不对？还是本地 pip 包？

**typegraph ❌**：
1. config 路径写死 `D:\AI\RedCode\plugins\typegraph-mcp\server.ts` → `D:\AI\RedCode` 在家不存在
2. 已改成相对路径：`npx tsx ./plugins/typegraph-mcp/server.ts`（redcode.jsonc + .opencode/opencode.jsonc 都改了）
3. 但 `D:\AI\KLX\RedCode\plugins\typegraph-mcp\` 目录只有 `node_modules/`，**没有 server.ts 源码**
4. `.opencode/plugins/typegraph-mcp/` 也只有 `node_modules/`，同样没源码
5. 需要主人从公司拷源码过来

**codegraph ✅**：
- `npx -y @colbymchenry/codegraph serve --mcp` 正常工作（npm 自动装包）

### 待办（主人明天回公司）
1. 确认 `jcodemunch-mcp` 包来源（PyPI 不存在），是内部包还是换个包名？
2. 把公司 `typegraph-mcp` 源码（server.ts 等）拷到家里的 `plugins/typegraph-mcp/`
3. 确认 browsermcp-extension 是否要配进 redcode.jsonc（目前没配）
4. 0.3.10 CHANGELOG 章节措辞修正（"集成" → "接入外部 MCP"）
