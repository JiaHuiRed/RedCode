## 智敏日记

## 小宋日记

- 技术建议前先过源码/CHANGELOG：提 GUI 改进前先看 CHANGELOG 最近的改动，别重复已有的功能（仓鼠+Mona 动画已经是 streaming 指示器了，不要再建议打字光标）
- GUI 0.4.1 头像功能：settings.tsx 加 userProfile/assistantProfile，message-part.tsx/css 气泡+头像，message-timeline.tsx 助手头像，settings-general.tsx 上传 UI，avatar.tsx 加 medium 尺寸
- session.tsx 拆分 4 个模块（history-loader/review-diff/message-nav/keyboard），主文件 1667→1623 行
- **"请选择智能体和模型" 弹窗在 0.4.1 再次出现（第 5 次）**：规律是每次改 message-timeline.tsx/submit.ts 等渲染路径时触发。根因是 `providers.ready()` 的 `all || connected` 条件过于宽松。敏敏修了（`&&`）。教训：改消息/渲染相关代码后必须在数据加载未完成状态下验证提交流程
- **git commit --amend 在 PS 5.1 下行为异常**：每次 amend 都创建新 commit 而非替换 HEAD，导致历史出现重复 commit。原因待查（可能是 staged 文件状态或 pre-commit hook 影响）。workaround：用 `git reset --soft <base>` + `git commit` 替代 amend
- 仓鼠浅色黑标：hamster.png 在浅色模式显示黑色锯齿边缘，用 `mix-blend-mode: screen` + `background: var(--surface-base)` 容器修复
